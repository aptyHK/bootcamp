public final class FinalClass {
  // if I set final for the class
  // Other class cannot extend this class
  // if try to extend, will have error:
  // The type ChildClass cannot subclass the final class FinalClassJava(16777529)
  
}
